{
  "name": "stable-center-backend",
  "version": "2.0.0",
  "description": "Secure READ-ONLY API for 1inch Fusion quotes and status (Non-custodial compliant)",
  "main": "server.ts",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "ts-node server.ts",
    "build": "tsc --project tsconfig.json",
    "build:start": "npm run build && npm start",
    "vercel-build": "echo 'No build step needed for serverless'",
    "test": "node test/full-cross-chain-test.js",
    "test:defi": "node test/defi-api-test.js",
    "setup": "node setup-env.js",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "defi",
    "cross-chain",
    "swap",
    "1inch",
    "fusion",
    "vercel",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^17.2.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/node": "^24.1.0",
    "@vercel/node": "^3.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}